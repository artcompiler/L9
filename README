OVERVIEW

L9 is a language of translating JavaScript ASTs (e.g. L7) into SVG ASTs with
pretty printing semantics. 

L9 is written in L0. 

BUILDING

To build:

   cd build
   ./make.sh

This produces the file:

   ../bin/l9c.swf


USING

To run:

    ./comp.sh some.js.l7

or

    avmshell ../bin/l9c.swf some.js.l7

where avmshell is the name of a recent build of Tamarin.

This produces the file:

    some.js.l7.l9

