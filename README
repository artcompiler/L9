OVERVIEW

L9 is a language of translating JavaScript ASTs into SVG ASTs with pretty printing semantics. 

L9 is written in L0. 

BUILDING

To build:

   cd build
   ./make.sh

This produces the file:

   ../bin/l9c.swf


USING

To run:

    ./comp.sh some.js.ast

or

    avmshell ../bin/l9c.swf some.js.ast

where avmshell is the name of a recent build of Tamarin.


This produces the file:

    some.js.svg.ast

